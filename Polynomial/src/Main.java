/**
 * Main class to run and test Polynomial Interface and respective methods.
 */

public class Main {

  /**
   * Main method to run the program.
   *
   * @param args contains the supplied command-line arguments as an array of String objects.
   */
  public static void main(String[] args) {

    // constructs new term
    //Empty emptyTerm1 = new Empty();
    // constructs the polynomial
    // Polynomial testPoly1 = new PolynomialImpl();
    //Polynomial testPoly2 = new PolynomialImpl("3x^2 +4x^1 -3");
    //System.out.println(testPoly2.toString());

    //Polynomial testPoly3 = new PolynomialImpl("0");
    //System.out.println(testPoly3.toString());


    //System.out.println(testPoly3.add(testPoly2));
    // adds terms to the polynomial
    //testPoly1.addTerm(-3, 2);
    //testPoly1.addTerm(2, 1);
    //testPoly1.addTerm(-4, 3);
    //testPoly1.addTerm(3, 0);
    //testPoly1.addTerm(8, 4);
    //testPoly1.addTerm(3, 5);
    //testPoly1.addTerm(-12, 8);
    //testPoly1.addTerm(8, 6);

    // prints polynomial
    //System.out.println(testPoly1.toString());

    // gets degree from the polynomial before removal
    //System.out.println(testPoly1.getDegree());

    // removes terms from the polynomial
    //testPoly1.removeTerm(3);

    // prints polynomial
    //System.out.println(testPoly1.toString());

    // gets degree from the polynomial after removal
    //System.out.println(testPoly1.getDegree());

    // gets coefficient from the polynomial
    //System.out.println(testPoly1.getCoefficient(2));

    // evaluates the polynomial
    //System.out.println(testPoly1.evaluate(4));
  }
}